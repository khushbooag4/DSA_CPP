#include<bits/stdc++.h>
using namespace std;
//Given a tree, the ith node is associated with a weight weight[i]
//no pair of vertices such that one is the ancestor of the 

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    
    return 0;
}